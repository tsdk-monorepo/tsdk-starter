{
  "name": "tsdk-quickstart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "ready": "pnpm --filter=server sync-sdk",
    "prepare": "is-ci || husky install"
  },
  "dependencies": {
    "is-ci": "^3.0.1"
  }
}
